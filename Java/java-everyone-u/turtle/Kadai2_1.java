public class Kadai2_1 {
    public static void main(String[] args){
        Species[] s = new Species[50];
        s[0] = new Species(0, 0, 0);  // 始まりの種(name, birth, ancestor)

        int end_year = 10;  // 世界の終わり
        double p = 0.3, q = 0.4;      // 分岐、絶滅、生存(1-p+q)
        int count_species = 1;        // 現時点の生存種の数(最初は1種)

    }
}
